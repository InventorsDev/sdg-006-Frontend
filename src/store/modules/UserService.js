import * as types from '../types'
import axios from '../../axios';
import router from '../../router'

const state = {
    userService : {
        userProfile,
        // rename userService to doctor specialist
    },
}